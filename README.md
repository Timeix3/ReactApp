# ReactApp

## CI

GitHub Actions runs the frontend ESLint checks, tests, and production build, plus
the backend Ruff checks, pytest suite, and Docker image build for pushes to
`main`, pull requests, and manual runs.